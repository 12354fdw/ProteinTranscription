{
  "name": "proteintranscription",
  "version": "1.0.0",
  "description": "Transcribes a DNA sequence to RNA and translates it into an amino acid (protein) chain.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "start": "tsc && node dist/index.js",
    "build:win": "esbuild src/index.ts --bundle --platform=node --format=cjs --target=node18 --outfile=pkg/bundle.cjs && pkg pkg/bundle.cjs --target node24-win-x64 --output build/ProteinTranscription.exe --compress=GZip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "@yao-pkg/pkg": "^6.22.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.9.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  },
  "dependencies": {
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.11.0"
  }
}
